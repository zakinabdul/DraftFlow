services:
  app:
    build:
      context: ../..
      dockerfile: backend/.devcontainer/Dockerfile
    image: draftflow_backend:dev
    restart: unless-stopped
    env_file: ../envs/.env
    ports:
      - "8080:8080"
    volumes:
      - ../..:/workspace
